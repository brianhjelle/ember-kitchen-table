{{#if (has-block)}}
  {{yield 
    (hash thead=(component "kitchen-thead") tbody=(component "kitchen-tbody"))
  }}
{{else}}
  <KitchenThead @cols={{cols}} />
  <KitchenTbody @cols={{cols}} @data={{data}} />
{{/if}}
