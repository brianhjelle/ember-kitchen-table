{{#if (has-block)}}
  {{yield (hash thead=(component "kitchen-column"))}}
{{else}}
  <tr>
    {{#each @columns as |col|}}
      <KitchenColumn @col={{col}}></KitchenColumn>
    {{/each}}
  </tr>
{{/if}}
