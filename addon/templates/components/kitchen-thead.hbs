{{#if (has-block)}}
  {{yield (hash thead=(component "kitchen-column"))}}
{{else}}
  <tr>
    {{#each cols as |col|}}
      <KitchenColumn @fieldToSort={{col.field}}>{{col.displayName}}</KitchenColumn>
    {{/each}}
  </tr>
{{/if}}
